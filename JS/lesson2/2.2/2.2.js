var numeric = prompt('Введіть будь-яке ціле число', '');

if (numeric > 0) {
    alert(1);
} else if (numeric < 0) {
    alert(-1);
} else {
    alert(0);
}

//var al = numeric > 0 ? 1 : numeric < 0 ? -1 : 0;
//alert(al);